Alfresco Share module to hide the "Download Original" button from the Quickshare page
=====================================================================================

The title pretty much explains it all.

Use `mvn package` to build an AMP file in the target directory.
